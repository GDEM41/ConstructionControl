<Window x:Class="ConstructionControl.CreateObjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Создать объект"
        Height="180"
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <StackPanel Margin="15">

        <TextBlock Text="Название объекта"
                   Margin="0,0,0,5"/>

        <TextBox x:Name="ObjectNameBox"
                 Margin="0,0,0,15"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button Content="Отмена"
                    Width="90"
                    Margin="5"
                    Click="Cancel_Click"/>

            <Button Content="ОК"
                    Width="90"
                    Margin="5"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="Ok_Click"/>
        </StackPanel>

    </StackPanel>
</Window>

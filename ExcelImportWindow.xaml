<Window x:Class="ConstructionControl.ExcelImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Импорт из Excel"
        Height="720"
        Width="1380"
        MinHeight="680"
        MinWidth="1220"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBgBrush}">

    <Window.Resources>
      

            <!-- Тень для выпадающего списка -->
            <DropShadowEffect x:Key="ComboShadow"
                      Color="Black"
                      Opacity="0.12"
                      BlurRadius="12"
                      ShadowDepth="2"/>

            <!-- дальше твои стили ModernComboBox, ModernRadio и т.д. -->

            <!-- ====== ComboBox стиль в стиле pill ====== -->
        <Style x:Key="PillComboBox" TargetType="ComboBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="Toggle"
                                  Grid.Column="2"
                                  Focusable="False"
                                  ClickMode="Press"
                                  IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                  Background="{TemplateBinding Background}"
                                  BorderBrush="{TemplateBinding BorderBrush}"
                                  BorderThickness="{TemplateBinding BorderThickness}"
                                  Padding="10,4"
                                  Cursor="Hand">
                                <DockPanel>
                                    <ContentPresenter VerticalAlignment="Center"/>
                                    <TextBlock Text="▾" Margin="8,0,0,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                </DockPanel>
                            </ToggleButton>

                            <Popup x:Name="Popup"
                           Placement="Bottom"
                           AllowsTransparency="True"
                           StaysOpen="False"
                           IsOpen="{TemplateBinding IsDropDownOpen}">
                                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="6" BorderBrush="{DynamicResource StrokeBrush}" BorderThickness="1">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12"/>

                            <ContentPresenter Grid.Column="0"
                  Margin="10,0,10,0"
                  VerticalAlignment="Center"
                  Content="{TemplateBinding SelectionBoxItem}"
                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                  RecognizesAccessKey="True"/>

                            <!-- Кнопка раскрытия -->
                            <ToggleButton x:Name="PART_ToggleButton"
              Width="32"
              Height="32"
              Background="Transparent"
              BorderThickness="0"
              Focusable="False"
              HorizontalAlignment="Right"
              VerticalAlignment="Center"
              Cursor="Hand"
              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ArrowBg"
                    CornerRadius="6"
                    Padding="6"
                    Background="Transparent">
                                            <Path x:Name="ArrowIcon"
                      Width="12"
                      Height="8"
                      Fill="{DynamicResource TextSecondaryBrush}"
                      Data="M 0 0 L 12 0 L 6 8 Z"/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <!-- Hover -->
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ArrowBg" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                                                <Setter TargetName="ArrowIcon" Property="Fill" Value="{DynamicResource TextBrush}"/>
                                            </Trigger>

                                            <!-- Pressed -->
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="ArrowBg" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                                                <Setter TargetName="ArrowIcon" Property="Fill" Value="{DynamicResource TextBrush}"/>
                                            </Trigger>

                                            <!-- Checked (dropdown открыт) -->
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ArrowBg" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                                                <Setter TargetName="ArrowIcon" Property="Fill" Value="{DynamicResource TextBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>



                            <Popup x:Name="PART_Popup"
                           Placement="Bottom"
                           AllowsTransparency="True"
                           Focusable="False"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           PopupAnimation="Fade">

                                <Border Margin="0,4,0,0"
                                Background="{DynamicResource SurfaceBrush}"
                                BorderBrush="{DynamicResource StrokeBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                SnapsToDevicePixels="True"
                                Padding="4"
                                Effect="{DynamicResource ComboShadow}">
                                    <ScrollViewer CanContentScroll="True"
                                          VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBd"
                                    CornerRadius="6"
                                    Background="Transparent"
                                    Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBd" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource SurfaceBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="EditableModernComboBox"
               TargetType="ComboBox"
               BasedOn="{StaticResource ModernComboBox}">
            <Setter Property="IsEditable" Value="True"/>
            <Setter Property="IsTextSearchEnabled" Value="False"/>
            <Setter Property="StaysOpenOnEdit" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border x:Name="Bd"
                                    Grid.ColumnSpan="2"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12"/>

                            <ContentPresenter x:Name="ContentSite"
                                              Grid.Column="0"
                                              Margin="10,0,10,0"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              RecognizesAccessKey="True"/>

                            <TextBox x:Name="PART_EditableTextBox"
                                     Grid.Column="0"
                                     Margin="10,0,4,0"
                                     VerticalContentAlignment="Center"
                                     HorizontalContentAlignment="Left"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}"
                                     Visibility="Hidden"/>

                            <ToggleButton x:Name="PART_ToggleButton"
                                          Grid.Column="1"
                                          Width="32"
                                          Height="32"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          HorizontalAlignment="Right"
                                          VerticalAlignment="Center"
                                          Cursor="Hand"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ArrowBg"
                                                CornerRadius="6"
                                                Padding="6"
                                                Background="Transparent">
                                            <Path x:Name="ArrowIcon"
                                                  Width="12"
                                                  Height="8"
                                                  Fill="{DynamicResource TextSecondaryBrush}"
                                                  Data="M 0 0 L 12 0 L 6 8 Z"/>
                                        </Border>

                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ArrowBg" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                                                <Setter TargetName="ArrowIcon" Property="Fill" Value="{DynamicResource TextBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="ArrowBg" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                                                <Setter TargetName="ArrowIcon" Property="Fill" Value="{DynamicResource TextBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ArrowBg" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                                                <Setter TargetName="ArrowIcon" Property="Fill" Value="{DynamicResource TextBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   PopupAnimation="Fade">
                                <Border Margin="0,4,0,0"
                                        Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource StrokeBrush}"
                                        BorderThickness="1"
                                        CornerRadius="12"
                                        SnapsToDevicePixels="True"
                                        Padding="4"
                                        Effect="{DynamicResource ComboShadow}">
                                    <ScrollViewer CanContentScroll="True"
                                                  VerticalScrollBarVisibility="Auto">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ModernRadio" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd"
                        Background="{DynamicResource StrokeBrush}"
                        BorderBrush="{DynamicResource StrokeBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="10,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrushPressed}"/>
                                <Setter Property="Foreground" Value="{DynamicResource SurfaceBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Popup: тенюшка и нормальное позиционирование -->
        <Style TargetType="Popup">
            <Setter Property="AllowsTransparency" Value="True"/>
        </Style>



        <Style x:Key="PillButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="Background" Value="{DynamicResource StrokeBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                        CornerRadius="12"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButton" BasedOn="{StaticResource PillButton}" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBrushPressed}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                        CornerRadius="12"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource PrimaryBrushHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource PrimaryBrushPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SquareIconButton" TargetType="Button">
            <Setter Property="Width" Value="38"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Background" Value="{DynamicResource StrokeBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                        CornerRadius="10"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource StrokeBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ImportSectionCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource StrokeBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="12"/>
        </Style>

        <Style x:Key="ImportPanelTitle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        </Style>

        <Style x:Key="ImportSubtleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="ImportSheetsListBox" TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,6"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Root"
                                            Background="{DynamicResource SurfaceBrush}"
                                            BorderBrush="{DynamicResource StrokeBrush}"
                                            BorderThickness="1"
                                            CornerRadius="10"
                                            Padding="10,8">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Root" Property="Background" Value="{DynamicResource SelectedBrush}"/>
                                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>


    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource StrokeBrush}"
            BorderThickness="1"
            CornerRadius="18"
            Padding="18"
            Margin="12">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ===== Заголовок ===== -->
            <Border Style="{StaticResource ImportSectionCard}"
                    Margin="0,0,0,14">
                <StackPanel>
                    <TextBlock Text="Импорт из Excel"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}"/>
                    <TextBlock x:Name="FilePathText"
                               Margin="0,6,0,0"
                               Style="{StaticResource ImportSubtleText}"
                               FontSize="12"/>
                </StackPanel>
            </Border>

            <!-- ===== Основная область ===== -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="290"/>
                    <ColumnDefinition Width="14"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Листы Excel -->
                <Border Style="{StaticResource ImportSectionCard}">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Листы Excel"
                                   Style="{StaticResource ImportPanelTitle}"
                                   Margin="0,0,0,8"/>

                        <ListBox x:Name="SheetsList"
                                 Grid.Row="1"
                                 MinHeight="420"
                                 Style="{StaticResource ImportSheetsListBox}"
                                 SelectionChanged="SheetsList_SelectionChanged"/>
                    </Grid>
                </Border>

                <!-- Разделитель -->
                <GridSplitter Grid.Column="1"
                              Width="10"
                              Background="Transparent"
                              HorizontalAlignment="Stretch"/>

                <!-- Предпросмотр -->
                <Border Grid.Column="2"
                        Style="{StaticResource ImportSectionCard}">

                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Предпросмотр листа"
                                   Style="{StaticResource ImportPanelTitle}"
                                   Margin="0,0,0,8"/>

                        <DataGrid x:Name="PreviewGrid"
                                  Grid.Row="1"
                                  Margin="0,0,0,12"
                                  AutoGenerateColumns="True"
                                  SelectionUnit="Cell"
                                  SelectionMode="Extended"
                                  HeadersVisibility="Column"
                                  GridLinesVisibility="All"
                                  HorizontalGridLinesBrush="{DynamicResource StrokeBrush}"
                                  VerticalGridLinesBrush="{DynamicResource StrokeBrush}"
                                  Background="{DynamicResource SurfaceBrush}"
                                  BorderThickness="0"
                                  SelectedCellsChanged="PreviewGrid_SelectedCellsChanged"/>

                        <!-- Кнопки выбора -->
                        <StackPanel Grid.Row="2"
                                    Margin="0,0,0,0"
                                    HorizontalAlignment="Left">
                            <WrapPanel Margin="0,0,0,8">

                                <!-- Группа 1 -->
                                <Button Content="🧾 Позиция" Tag="Position" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="🧱 Наименование" Tag="Material" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="🏷 СТБ" Tag="Stb" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="📐 Ед. изм" Tag="Unit" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="📦 Объем" Tag="Volume" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>

                                <!-- разделитель -->
                                <Rectangle Width="20"/>

                                <!-- Группа 2 -->
                                <Button Content="📅 Дата" Tag="Date" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="🚚 ТТН" Tag="Ttn" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="🏭 Поставщик" Tag="Supplier" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="📄 Паспорт" Tag="Passport" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>
                                <Button Content="🔢 Кол-во" Tag="Quantity" Style="{StaticResource PillButton}" Margin="2" Click="SelectCell_Click"/>

                            </WrapPanel>

                            <WrapPanel VerticalAlignment="Center">
                                <TextBlock Text="Сводная таблица:"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0"/>
                                <TextBlock Text="(Ctrl — добавить ячейки, Shift — диапазон в одной строке)"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="0,0,12,0"/>
                                <TextBlock Text="Блок"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                <ComboBox x:Name="BlockSelector"
                                          Style="{StaticResource ModernComboBox}"
                                          Width="90"
                                          Margin="0,0,10,0"
                                          SelectionChanged="BlockSelector_SelectionChanged"/>

                                <TextBlock Text="Отметка"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                <ComboBox x:Name="MarkSelector"
                                          Style="{StaticResource EditableModernComboBox}"
                                          Width="150"
                                          Margin="0,0,10,0"/>
                                <Button Content="📊 Потребность"
                                        Tag="DemandColumn"
                                        Style="{StaticResource PillButton}"
                                        Click="SelectCell_Click"/>
                            </WrapPanel>
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <!-- ===== Низ ===== -->
            <Grid Grid.Row="2" Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Style="{StaticResource ImportSectionCard}"
                        Margin="0,0,12,0"
                        Padding="12,10">
                    <TextBlock x:Name="SelectedCellText"
                               Style="{StaticResource ImportSubtleText}"
                               FontSize="12"
                               Text="Ячейка не выбрана"/>
                </Border>




                <!-- ===== Нижняя панель: тип импорта + шаблоны ===== -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Border Style="{StaticResource ImportSectionCard}"
                            Margin="0,0,12,0"
                            Padding="12,10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <RadioButton x:Name="MainRadio"
                                         Style="{StaticResource ModernRadio}"
                                         Content="Основные"
                                         Margin="0,0,8,0"
                                         IsChecked="True"/>

                            <RadioButton x:Name="LowCostRadio"
                                         Style="{StaticResource ModernRadio}"
                                         Content="Малоценка"
                                         Margin="0,0,8,0"/>

                            <RadioButton x:Name="InternalRadio"
                                         Style="{StaticResource ModernRadio}"
                                         Content="Внутренние"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ImportSectionCard}"
                            Margin="0,0,12,0"
                            Padding="12,10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Шаблон:"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>

                            <ComboBox x:Name="TemplatesBox"
                                      Style="{StaticResource ModernComboBox}"
                                      Width="250"/>

                            <Button Content="⟳" Style="{StaticResource SquareIconButton}" Click="RefreshTemplates_Click"/>
                            <Button Content="✖" Style="{StaticResource SquareIconButton}" Click="DeleteTemplate_Click"/>
                            <TextBlock x:Name="CurrentTemplateText"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource ImportSectionCard}"
                            Padding="12,10">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="⚙ Применить " Style="{StaticResource PillButton}" Margin="0" Click="ApplyTemplate_Click"/>
                            <Button Content="💾 Сохранить " Style="{StaticResource PillButton}" Click="SaveTemplate_Click"/>
                            <Button Content="🚀 Импорт " Style="{StaticResource PrimaryButton}" Click="Import_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>








            </Grid>
        </Grid>
    </Border>
</Window>
